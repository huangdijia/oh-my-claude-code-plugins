# Claude Code 插件

为 [Claude Code](https://claude.ai/code) 提供强大开发工作流的自定义智能体和斜杠命令精选集合。[英文版](README.md)

## 🌟 特性

### 🤖 专业 AI 智能体

**开发工作流智能体：**

- **strategic-planner** - 专业软件架构师，负责功能规划和技术设计
- **task-executor** - 精确工程师，负责原子任务实现
- **code-reviewer** - 自动化代码质量和安全审查专家

**支持智能体：**

- **steering-architect** - 项目分析和文档架构师
- **data-scientist** - SQL 查询、BigQuery 操作和数据分析
- **debugger** - 错误排查和根因分析
- **prd-writer** - 产品需求文档生成

### ⚡ Git 工作流命令

**GitHub：**

- `/devtools:pr:create` - 创建 Pull Request，支持自动分支管理
- `/devtools:pr:review` - 全面的 PR 代码审查，提供结构化分析

**GitLab：**

- `/devtools:mr:create` - 创建 Merge Request
- `/devtools:mr:review` - MR 审查，支持自定义范围（性能、安全、风格）

**Git：**

- `/devtools:commit:create` - 根据暂存更改生成语义化提交

## 📦 安装

### 从 GitHub 安装

1. 在 Claude Code 中运行以下命令：

```bash
/plugin marketplace add huangdijia/oh-my-claude-code-plugins
```

2. 安装 `devtools` 插件：

```bash
/plugin install devtools@oh-my-claude-code-plugins
```

## 🚀 快速开始

### 规范驱动的开发工作流

本插件实现了三阶段开发工作流：

#### 1. 规划阶段

使用战略规划师设计您的功能：

```markdown
使用 strategic-planner 智能体帮我规划用户认证功能
```

这将在 `specs/<功能名称>/` 中创建结构化规范：

- `requirements.md` - 包含验收标准的用户故事
- `design.md` - 包含图表的技术蓝图
- `tasks.md` - 细粒度任务清单

#### 2. 执行阶段

以外科手术般的精确度逐个执行任务：

```markdown
使用 task-executor 智能体从 specs/user-auth/tasks.md 实现下一个任务
```

智能体将：

- 每次执行恰好一个任务
- 更新任务复选框 `[ ]` → `[x]`
- 在标记完成前运行测试
- 支持无人值守执行的自主模式

#### 3. 审查阶段

自动化质量保证：

```bash
使用 code-reviewer 智能体审查我的最近更改
```

### 使用斜杠命令

**创建提交：**

```bash
/devtools:commit:create
```

**创建和审查 PR：**

```bash
/devtools:pr:create
/devtools:pr:review 123
```

**指定范围的审查：**

```bash
/devtools:mr:review 456 performance
/devtools:mr:review 789 security
```

## 📚 文档

### 智能体工作流模式

插件实现了复杂的多智能体系统：

```bash
┌─────────────────────────────────────────────────────────┐
│  阶段 1：战略规划                                        │
│  → 分析需求                                             │
│  → 创建技术设计                                         │
│  → 生成任务清单                                         │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────┐
│  阶段 2：任务执行                                        │
│  → 执行一个原子任务                                     │
│  → 运行测试和验证                                       │
│  → 标记任务完成                                         │
│  → 重复下一个任务                                       │
└────────────────┬────────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────────┐
│  阶段 3：质量保证                                        │
│  → 审查代码质量                                         │
│  → 检查安全问题                                         │
│  → 验证可维护性                                         │
└─────────────────────────────────────────────────────────┘
```

### 项目初始化

使用 steering-architect 智能体设置项目治理：

```markdown
使用 steering-architect 智能体分析这个项目
```

这将创建 `.ai-rules/` 文件：

- `product.md` - 产品愿景和目标
- `tech.md` - 技术栈和命令
- `structure.md` - 文件组织约定

### 工具限制

每个智能体都有特定的工具访问权限以确保安全：

| 智能体 | 工具 | 代码执行 |
|-------|-------|---------|
| strategic-planner | file_edit, file_search, web_search | ❌ 否 |
| task-executor | file_edit, bash, file_search | ✅ 是 |
| code-reviewer | file_search, bash, file_edit | ✅ 是 |
| steering-architect | file_edit, file_search, bash | ✅ 是 |
| data-scientist | bash, file_search, file_edit | ✅ 是 |
| debugger | file_search, file_edit, bash | ✅ 是 |
| prd-writer | file_edit, web_search, file_search | ❌ 否 |

## 🏗️ 项目结构

```bash
claude-marketplace/
├── .claude-plugin/
│   └── marketplace.json          # 插件注册表
└── plugins/
    └── devtools/                 # 主插件
        ├── agents/               # AI 智能体定义
        │   ├── code-reviewer.md
        │   ├── data-scientist.md
        │   ├── debuger.md
        │   ├── prd-writer.md
        │   ├── steering-architect.md
        │   ├── strategic-planner.md
        │   └── task-executor.md
        ├── commands/             # 斜杠命令
        │   ├── commit/
        │   │   └── create.md
        │   ├── mr/
        │   │   ├── create.md
        │   │   └── review.md
        │   └── pr/
        │       ├── create.md
        │       └── review.md
        └── hooks/                # 事件钩子
```

## 🎯 使用场景

### 功能开发

```text
1. 规划：使用 strategic-planner 设计功能
2. 构建：使用 task-executor 实现任务
3. 审查：使用 code-reviewer 验证质量
```

### 代码审查自动化

```bash
# 专注于安全的自动化 PR 审查
/devtools:pr:review 123 security
```

### 项目设置

```markdown
# 初始化项目文档
使用 steering-architect 智能体设置项目标准
```

### 数据分析

```markdown
# SQL 和数据分析
使用 data-scientist 智能体从数据库分析用户指标
```

## 🌐 语言支持

多个组件针对目标用户群输出中文：

- 代码审查反馈
- PR/MR 审查分析
- 智能体指令（双语）

**注意：** debugger 智能体的文件名是 `debuger.md`（没有 'g'），而智能体名称是 `debugger`。

## 📖 高级用法

### 自主任务执行

启用自主模式以进行无人值守的任务执行：

```markdown
使用 task-executor 智能体在自主模式下完成所有剩余任务
```

智能体将：

- 按顺序执行任务而不停止
- 自动标记任务完成
- 仅在出错或完成时暂停

### 自定义范围审查

将代码审查聚焦于特定方面：

```bash
/devtools:pr:review 123 performance    # 关注性能问题
/devtools:pr:review 456 security       # 关注安全漏洞
/devtools:pr:review 789 style          # 关注代码风格和约定
```

### 规范管理

使用规范目录组织功能：

```bash
specs/
├── user-authentication/
│   ├── requirements.md
│   ├── design.md
│   └── tasks.md
├── payment-integration/
│   ├── requirements.md
│   ├── design.md
│   └── tasks.md
```

## ⚙️ 配置

插件在 `.claude-plugin/marketplace.json` 中注册：

```json
{
  "plugins": [
    {
      "name": "devtools",
      "source": "./plugins/devtools",
      "description": "Plugin under development",
      "category": "development"
    }
  ]
}
```

## 🤝 贡献

欢迎贡献！本仓库旨在成为 Claude Code 插件的市场。

### 添加新智能体

1. 在 `plugins/devtools/agents/` 中创建新的 `.md` 文件
2. 添加包含名称、描述和工具的 YAML 前置元数据
3. 在 markdown 中定义智能体指令

### 添加新命令

1. 在 `plugins/devtools/commands/<类别>/` 中创建新的 `.md` 文件
2. 添加包含 allowed-tools 和描述的 YAML 前置元数据
3. 使用动态上下文定义命令提示

## 📄 许可证

MIT 许可证 - 详见 LICENSE 文件

## 🔗 链接

- [Claude Code 文档](https://docs.claude.com/en/docs/claude-code)
- [插件开发指南](https://docs.claude.com/en/docs/claude-code/plugins)

## ✨ 致谢

由 [@huangdijia](https://github.com/huangdijia) 开发

---

**注意：** 本插件需要 [Claude Code](https://claude.ai/code) 才能运行。请确保已安装并配置 Claude Code。
